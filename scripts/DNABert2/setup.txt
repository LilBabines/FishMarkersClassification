1 - install python packages


pip install -r requirements

------ or -----

pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
pip install transformers[torch]
pip install hydra-core
pip install datasets
pip install scikit-learn
pip install torchmetrics
pip install matplotlib
pip install einops


2 - depends on your hardware you should uninstall triton, regarding to github triton issues : https://github.com/MAGICS-LAB/DNABERT_2/issues?q=is%3Aissue%20triton


3 - If using a recent python version, you will need standard-imghdr package to use Tensorboard:

pip install standard-imghdr, it might be a better way to do it

